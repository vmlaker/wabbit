#!/bin/sh

ROOT=@WORKING_DIR@
GUNICORN=@GUNICORN_BIN@
cd $ROOT
exec chpst -u@USER@ @GUNICORN_BIN@ @APP_MODULE@ --workers @gunicorn_workers@ --threads @gunicorn_threads@ --bind 127.0.0.1:@gunicorn_port@
